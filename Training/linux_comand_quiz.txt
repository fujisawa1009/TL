# 基本操作
# 1. カレントディレクトリの場所を確認する。
# 2. 自分のユーザー名を確認する。
# 3. システムの現在時刻を確認する。
# 4. 現在使用しているシェルの種類を確認する。
# 5. 最後に実行したコマンドの終了ステータスを確認する。
# 6. シェル変数PATHの値を確認する。

# ファイル・フォルダ操作
# 7. 'work'というフォルダを作成する。
# 8. 'work'フォルダに'note.txt'という空のファイルを作成する。
# 9. 'note.txt'に'Hello, Linux!'という文字列を出力する。
# 10. 'note.txt'の内容をターミナルに表示する。
# 11. 'note.txt'に新しい行で'This is a test.'という文字列を追加する。
# 12. 'note.txt'の行数を表示する。
# 13. 'work'フォルダ内に'backup'という名前のフォルダを作成する。
# 14. 'note.txt'を'backup'フォルダにコピーする。
# 15. 'backup'フォルダ内の'note.txt'の内容を確認する。

# ファイル・フォルダの名前変更や削除
# 16. 'note.txt'を'memo.txt'という名前に変更する。
# 17. 'backup'フォルダを'archive'という名前に変更する。
# 18. 'archive'フォルダを削除する。
# 19. 空フォルダを作成し、削除する。

# 権限操作
# 20. 'memo.txt'を読み取り専用に設定する。
# 21. 'memo.txt'に書き込み権限を追加する。
# 22. 'work'フォルダ内のすべてのファイルに実行権限を追加する。
# 23. 'memo.txt'の所有者を確認する。
# 24. 'memo.txt'の所有者を'sudo'権限を使って'root'に変更する。

# 階層移動
# 25. ホームディレクトリに移動する。
# 26. 'work'フォルダに戻る。

# ファイル検索
# 27. ホームディレクトリから'memo.txt'を検索する。
# 28. カレントディレクトリでサイズが1KB以上のファイルを検索する。

# ファイル内容検索
# 29. 'memo.txt'に含まれる'Linux'という単語を検索する。
# 30. 'memo.txt'に含まれる'Test'を大文字小文字を区別せず検索する。

# Vimでの編集
# 31. Vimを使って'memo.txt'の最初の行に'Welcome!'という文字列を追加する。

# システム情報の確認
# 32. 使用中メモリ量を確認する。
# 33. カーネルバージョンを確認する。
# 34. 現在ログイン中のユーザー一覧を確認する。
# 35. システムの稼働時間を確認する。

# あいまい検索
# 36. '/usr/bin'ディレクトリ内で'ls'から始まるコマンドを探す。

# sudoコマンド
# 37. '/etc/passwd'ファイルを'sudo'を使って表示する。

# その他の課題
# 38. 現在のディスク使用量を確認する。
# 39. ネットワークの現在の状態を確認する。
# 40. カレントディレクトリのファイルをサイズ順に並べて表示する。
# 41. 'ls'コマンドの結果を'list.txt'というファイルに保存する。
# 42. カレントディレクトリ内で拡張子が'.txt'のすべてのファイルを削除する。
# 43. 実行中のプロセスの一覧を確認する。
# 44. 特定のプロセス（例: ssh）の情報を表示する。

# 応用課題
# 45. 'ls'コマンドの結果を行番号付きで表示する。
# 46. 特定のファイルの最後の10行を表示する。
# 47. ファイルサイズが大きい順に10個のファイルを表示する。
# 48. 実行中のbashプロセスを終了する。
# 49. カレントディレクトリ内のすべてのフォルダを表示する。
# 50. カレントディレクトリ内のすべてのファイルとフォルダの個数を表示する。
